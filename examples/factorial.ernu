factorial = function (n)
  "Calculates the factorial of n recursively"
  if n == 0 do
    1
  else
    n * factorial(n - 1)
  end
end

import("dir/definitions1.ernu")

for i in range(coolVariable) do
  print(i, factorial(i))
  if i >= coolVariable2 do
    print("starting to overflow? get a better language!")
  end
end